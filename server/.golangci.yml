version: "2"

linters:
  default: standard
  enable:
    - revive
    - gosec
    - bodyclose
    - sqlclosecheck
    - rowserrcheck
    - contextcheck
    - noctx

formatters:
  enable:
    - gofumpt
  settings:
    gofumpt:
      module-path: vpsbackupmanager
